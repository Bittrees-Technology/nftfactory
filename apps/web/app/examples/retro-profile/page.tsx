import { pageMetadata } from '../../../lib/seo';
import RetroProfile from './retro-profile';
export const metadata = pageMetadata('B0B0’s after-hours internet room', 'A retro, experimental creator-page example on NFTFactory.', '/examples/retro-profile', true);
export default function ExampleProfilePage() { return <RetroProfile />; }
